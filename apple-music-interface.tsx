"use client"

import { SongwriterTool } from "@/components/SongwriterTool"

export default function Component() {
  // Use the real SongwriterTool as the main entry point for the new UI
  return <SongwriterTool />;
}
